{
  "name": "b2w-desafio",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.11.4",
    "@testing-library/react": "^11.1.0",
    "@testing-library/user-event": "^12.1.10",
    "axios": "^0.22.0",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-loader-spinner": "^4.0.0",
    "react-modal": "^3.14.3",
    "react-scripts": "4.0.3",
    "react-toastify": "^8.0.3",
    "styled-components": "^5.3.1",
    "web-vitals": "^1.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "start:fire": "yarn set-environment --fire && react-scripts start",
    "start:water": "yarn set-environment --water && react-scripts start",
    "start:air": "yarn set-environment --air && react-scripts start",
    "build:fire": "yarn set-environment --fire && react-scripts build",
    "build:water": "yarn set-environment --water && react-scripts build",
    "build:air": "yarn set-environment --air && react-scripts build",
    "set-environment": "node scripts/set-environment"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest",
      "plugin:prettier/recommended"
    ],
    "plugins": [
      "prettier"
    ],
    "rules": {
      "prettier/prettier": "error"
    }
  },
  "prettier": {
    "arrowParens": "avoid",
    "printWidth": 100,
    "semi": true,
    "singleQuote": true,
    "trailingComma": "none"
  },
  "devDependencies": {
    "eslint-config-prettier": "^8.0.0",
    "eslint-plugin-prettier": "^3.3.1",
    "prettier": "^2.2.1"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
